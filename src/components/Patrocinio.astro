<section class="patrocinio-section" id="patrocinio">
  <!-- Grid e Efeitos de Luz de Fundo (Estilo Tech) -->
  <div class="patrocinio-grid"></div>
  <div class="patrocinio-glow patrocinio-glow-1"></div>
  <div class="patrocinio-glow patrocinio-glow-2"></div>

  <div class="patrocinio-container">
    <div class="patrocinio-tag">
      OPORTUNIDADE PARA MARCAS
    </div>

    <h2>
      VENHA SER UM EXPOSITOR NO EVENTO MAIS RELEVANTE EM
      <span>GESTÃO DE FROTAS DA AMÉRICA LATINA</span>
    </h2>

    <p>
      Posicione sua marca diante de gestores, especialistas e empresas
      que movimentam o setor de frotas no Brasil.
    </p>

    <div class="patrocinio-actions">
      <a
        class="patrocinio-button whatsapp"
        href="https://wa.me/5511979718928?text=Olá!%20Gostaria%20de%20receber%20informações%20sobre%20patrocínio%20e%20exposição%20no%20PARAR%20Summit%2026."
        target="_blank"
        rel="noopener noreferrer"
      >
        <span class="whatsapp-icon" aria-hidden="true">
          <svg viewBox="0 0 32 32">
            <path
              d="M16.04 3C8.87 3 3.03 8.75 3.03 15.82c0 2.26.6 4.46 1.73 6.4L3 29l6.98-1.82a13.2 13.2 0 0 0 6.05 1.46h.01c7.17 0 13-5.75 13-12.82S23.21 3 16.04 3Zm0 23.48h-.01a11.1 11.1 0 0 1-5.66-1.54l-.4-.23-4.14 1.08 1.1-4.02-.26-.41a10.48 10.48 0 0 1-1.62-5.54c0-5.88 4.93-10.66 10.99-10.66s10.98 4.78 10.98 10.66-4.93 10.66-10.98 10.66Zm6.02-7.98c-.33-.16-1.95-.95-2.25-1.06-.3-.11-.52-.16-.74.16-.22.33-.85 1.06-1.04 1.28-.19.22-.38.25-.71.08-.33-.16-1.39-.5-2.65-1.6a9.85 9.85 0 0 1-1.84-2.25c-.19-.33-.02-.5.14-.67.15-.15.33-.38.49-.57.16-.19.22-.33.33-.55.11-.22.05-.41-.03-.57-.08-.16-.74-1.76-1.01-2.41-.27-.64-.54-.55-.74-.56h-.63c-.22 0-.57.08-.87.41-.3.33-1.14 1.1-1.14 2.69s1.17 3.12 1.34 3.34c.16.22 2.31 3.49 5.59 4.89.78.33 1.39.52 1.86.67.78.25 1.49.21 2.05.13.63-.09 1.95-.79 2.22-1.55.27-.76.27-1.41.19-1.55-.08-.14-.3-.22-.63-.38Z"
            />
          </svg>
        </span>

        FALAR SOBRE PATROCÍNIO
      </a>

      <a
        class="patrocinio-email"
        href="mailto:atendimento@parar.com.br?subject=Patrocínio%20PARAR%20Summit%2026"
      >
        atendimento@parar.com.br
      </a>
    </div>
  </div>
</section>

<style>
  .patrocinio-section {
    position: relative;
    overflow: hidden;
    padding: 100px 24px;
    background: #06171b; /* Fundo ligeiramente mais escuro para destacar o neon */
    font-family: "Raleway", sans-serif;
  }

  /* Malha de Grade Vetorial (Efeito Tech) */
  .patrocinio-grid {
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(rgba(0, 194, 100, 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0, 194, 100, 0.03) 1px, transparent 1px);
    background-size: 40px 40px;
    background-position: center;
    mask-image: radial-gradient(circle, black 40%, transparent 80%);
    -webkit-mask-image: radial-gradient(circle, black 40%, transparent 80%);
    pointer-events: none;
    z-index: 1;
  }

  .patrocinio-container {
    position: relative;
    z-index: 2;
    width: min(1120px, 100%);
    margin: 0 auto;
    text-align: center;
  }

  /* Badge com efeito de vidro (Glassmorphism) */
  .patrocinio-tag {
    width: fit-content;
    margin: 0 auto 32px;
    padding: 8px 20px;

    border: 1px solid rgba(0, 194, 100, 0.3);
    border-radius: 999px;

    background: rgba(0, 194, 100, 0.06);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    color: #00ff84;

    font-size: 11px;
    font-weight: 800;
    letter-spacing: 2px;
    box-shadow: inset 0 1px 1px rgba(255, 255, 255, 0.05);
  }

  .patrocinio-container h2 {
    max-width: 880px;
    margin: 0 auto;

    color: #ffffff;

    font-size: clamp(28px, 3.8vw, 46px);
    line-height: 1.1;
    font-weight: 900;
    letter-spacing: -1px;
    text-transform: uppercase;
  }

  .patrocinio-container h2 span {
    display: block;
    margin-top: 10px;
    color: #00ff84;
    text-shadow: 0 0 35px rgba(0, 255, 132, 0.35);
  }

  .patrocinio-container > p {
    max-width: 620px;
    margin: 24px auto 0;

    color: #a3c2c9;

    font-size: 18px;
    line-height: 1.6;
  }

  .patrocinio-actions {
    margin-top: 48px;

    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
  }

  /* Botão principal com Glow Pulsante */
  .patrocinio-button {
    min-height: 60px;
    padding: 0 36px;

    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 12px;

    border-radius: 999px;
    border: 1px solid rgba(0, 255, 132, 0.5);

    background: #00ff84;
    color: #06171b;

    font-size: 14px;
    font-weight: 900;
    letter-spacing: 1px;
    text-decoration: none;

    box-shadow: 
      0 12px 30px rgba(0, 255, 132, 0.25),
      0 0 60px rgba(0, 255, 132, 0.1);

    transition:
      transform 0.25s cubic-bezier(0.2, 1, 0.3, 1),
      box-shadow 0.25s ease,
      background-color 0.25s ease,
      border-color 0.25s ease,
      color 0.25s ease;
  }

  .patrocinio-button:hover {
    transform: translateY(-4px) scale(1.02);
    background: transparent;
    color: #00ff84;
    border-color: #00ff84;
    box-shadow: 
      0 20px 40px rgba(16, 30, 86, 0.3),
      0 0 40px rgba(12, 32, 59, 0.15);
  }

  .whatsapp-icon {
    width: 24px;
    height: 24px;
    display: block;
  }

  .whatsapp-icon svg {
    width: 100%;
    height: 100%;
    display: block;
    fill: currentColor;
  }

  .patrocinio-email {
    color: #a3c2c9;
    font-size: 15px;
    font-weight: 700;
    text-decoration: none;
    border-bottom: 1px solid rgba(163, 194, 201, 0.2);
    padding-bottom: 4px;

    transition:
      color 0.25s ease,
      border-color 0.25s ease;
  }

  .patrocinio-email:hover {
    color: #00ff84;
    border-color: rgba(0, 255, 132, 0.6);
  }

  /* Elementos de Luz Difusa (Aura Glow) */
  .patrocinio-glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(120px); /* Aumentado consideravelmente para suavizar a luz */
    pointer-events: none;
    opacity: 0.7;
    z-index: 1;
  }

  .patrocinio-glow-1 {
    width: 500px;
    height: 500px;
    left: -200px;
    top: -200px;

    background: radial-gradient(
      circle,
      rgba(7, 42, 85, 0.15),
      transparent 70%
    );
  }

  .patrocinio-glow-2 {
    width: 600px;
    height: 600px;
    right: -250px;
    bottom: -250px;

    background: radial-gradient(
      circle,
      rgba(0, 146, 218, 0.15),
      transparent 70%
    );
  }

  @media (max-width: 768px) {
    .patrocinio-section {
      padding: 80px 20px;
    }

    .patrocinio-container h2 {
      font-size: clamp(30px, 9vw, 40px);
      letter-spacing: -1px;
    }

    .patrocinio-container > p {
      margin-top: 20px;
      font-size: 15px;
    }

    .patrocinio-actions {
      margin-top: 40px;
    }

    .patrocinio-button {
      width: 100%;
      max-width: 360px;
      padding: 0 20px;
      font-size: 13px;
    }

    .patrocinio-email {
      font-size: 14px;
    }
  }
</style>